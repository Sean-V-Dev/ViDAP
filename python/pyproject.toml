[project]
name = "vidap-foundation"
version = "0.0.0"
description = "Local foundation dependencies for ViDAP."
requires-python = ">=3.14,<3.15"
dependencies = [
  "fastapi==0.141.1",
  "uvicorn==0.53.0",
]

[tool.uv]
package = false
